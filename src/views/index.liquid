<article>
  <h1>
    <span class="text">Welcome to CSWiki</span>
  </h1>
  <p>All articles:</p>
  <ul>
    {% for article in articles %}
      <li>
        <a href="{{ article.url }}">
          {{- article.title -}}
        </a>
      </li>
    {% endfor %}
  </ul>
</article>